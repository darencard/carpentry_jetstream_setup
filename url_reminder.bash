#!/usr/bin/env bash

# a script that prints URLs to terminal, for a reminder

# reminders of URLs for rstudio and Jupyter
echo -e "RStudio: http://$(hostname):8787/"
echo -e "Jupyter: https://$(hostname):8888/"
